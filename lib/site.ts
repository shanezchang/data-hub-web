export const API_DOCS = "https://api.lumina-core.cn/docs";
export const API_BASE = "https://api.lumina-core.cn";

// 站点规范域(canonical)——所有 SEO 绝对 URL(metadata / JSON-LD / sitemap /
// robots / IndexNow)的唯一来源。日后若改规范域(如 SHA-27 决定 apex/www 收口),
// 只改这一处即可全站对齐。注意:IndexNow 上报的是这里的最终 URL,避免 ping 跳转前地址。
export const SITE_URL = "https://console.lumina-core.cn";
